diff --git a/src/components/CallToAction.astro b/src/components/CallToAction.astro
index 3687311..47c1272 100644
--- a/src/components/CallToAction.astro
+++ b/src/components/CallToAction.astro
@@ -19,2 +19,3 @@ import { AppConfig } from "../constants"
             data-open-modal="trial-modal"
+            data-cta-location="final_cta_section"
             class="inline-flex cursor-pointer items-center rounded-pill bg-brand px-8 py-4 text-base font-semibold text-white shadow-soft transition-transform "
diff --git a/src/components/Guarantee.astro b/src/components/Guarantee.astro
index 1691a89..6ad7823 100644
--- a/src/components/Guarantee.astro
+++ b/src/components/Guarantee.astro
@@ -145,2 +145,3 @@ const steps = [
           data-open-modal="trial-modal"
+          data-cta-location="guarantee_section"
           class="inline-flex cursor-pointer items-center rounded-pill bg-brand px-8 py-4 text-base font-semibold text-white shadow-soft transition-transform "
diff --git a/src/components/Hero.astro b/src/components/Hero.astro
index 47e9aa3..06c2b8b 100644
--- a/src/components/Hero.astro
+++ b/src/components/Hero.astro
@@ -45,2 +45,3 @@ const isLocal =  isTanzania(countryCode);
             data-open-modal="trial-modal"
+            data-cta-location="hero"
             class="inline-flex cursor-pointer items-center rounded-pill bg-brand px-8 py-4 text-base font-semibold text-white shadow-soft transition-transform "
diff --git a/src/components/TrialLicenseForm.tsx b/src/components/TrialLicenseForm.tsx
index ab8004c..7bdd280 100644
--- a/src/components/TrialLicenseForm.tsx
+++ b/src/components/TrialLicenseForm.tsx
@@ -82,2 +82,22 @@ export function TrialLicenseForm({ id, country }: TrialLicenseFormProps) {
 
+      // GTM: fire the trial signup conversion event, carrying whichever
+      // plan (if any) was selected on the pricing page - see the click
+      // handler in Layout.astro that writes this to sessionStorage.
+      try {
+        const storedPlan = sessionStorage.getItem("rb_trial_plan");
+        const planData = storedPlan ? JSON.parse(storedPlan) : {};
+        (window as any).dataLayer = (window as any).dataLayer || [];
+        (window as any).dataLayer.push({
+          event: "trial_signup",
+          plan_name: planData.plan_name || "Not specified",
+          plan_price: planData.plan_price || null,
+          billing_period: planData.billing_period || null,
+          currency: planData.currency || null,
+          country,
+        });
+        sessionStorage.removeItem("rb_trial_plan");
+      } catch (gtmError) {
+        console.error("GTM tracking error:", gtmError);
+      }
+
       setStatus("Signing you in...");
diff --git a/src/components/components/subscriptionToggle.tsx b/src/components/components/subscriptionToggle.tsx
index e0c68db..61fe49c 100644
--- a/src/components/components/subscriptionToggle.tsx
+++ b/src/components/components/subscriptionToggle.tsx
@@ -61,2 +61,22 @@ export const Subscriptions = ({ plans, isLocal, countryCode }: Props) => {
 
+          // Same value that's rendered on screen - reused below so the
+          // GTM tracking attributes always match what the user saw.
+          const displayPrice = isCustom
+            ? "Custom"
+            : isLocal
+              ? price
+              : monthly
+                ? (price as string[])[1]
+                : (price as string[])[0];
+
+          const billingPeriod = isCustom
+            ? "custom"
+            : isLocal
+              ? monthly
+                ? "monthly"
+                : "yearly"
+              : monthly
+                ? "yearly"
+                : "monthly";
+
           return (
@@ -135,2 +155,7 @@ export const Subscriptions = ({ plans, isLocal, countryCode }: Props) => {
                 data-open-modal={isCustom ? "contact-us-modal" : "trial-modal"}
+                data-cta-location="pricing_page"
+                data-plan-name={!isCustom ? plan.name : undefined}
+                data-plan-price={!isCustom ? displayPrice : undefined}
+                data-billing-period={!isCustom ? billingPeriod : undefined}
+                data-currency={!isCustom ? (isLocal ? "TZS" : "USD") : undefined}
                 className={`mt-8 inline-flex cursor-pointer items-center justify-center rounded-pill px-6 py-3 text-sm font-semibold transition-transform hover:-translate-y-0.5 ${
diff --git a/src/layouts/AlternativesLayout.astro b/src/layouts/AlternativesLayout.astro
index c859d8e..5f71cae 100644
--- a/src/layouts/AlternativesLayout.astro
+++ b/src/layouts/AlternativesLayout.astro
@@ -52,2 +52,3 @@ const pageTitle = `Best ${entry.shortName} Alternative for SMEs | ${AppConfig.ap
             data-open-modal="trial-modal"
+            data-cta-location="alternatives_page_header"
             class="inline-flex cursor-pointer items-center rounded-pill bg-brand px-8 py-4 text-base font-semibold text-white shadow-soft transition-transform"
@@ -162,2 +163,3 @@ const pageTitle = `Best ${entry.shortName} Alternative for SMEs | ${AppConfig.ap
               data-open-modal="trial-modal"
+              data-cta-location="alternatives_page_footer"
               class="inline-flex cursor-pointer items-center rounded-pill bg-brand px-8 py-4 text-base font-semibold text-white shadow-soft transition-transform"
@@ -176,15 +178 @@ const pageTitle = `Best ${entry.shortName} Alternative for SMEs | ${AppConfig.ap
 <ContactUsModal id="contact-us-modal" title="Contact Us" />
-
-<script is:inline>
-  (function () {
-    document.addEventListener('click', function (e) {
-      const btn = e.target.closest('[data-open-modal]');
-      if (!btn) return;
-      const modalId = btn.getAttribute('data-open-modal');
-      const dialog = document.getElementById(modalId);
-      if (dialog && typeof dialog.showModal === 'function') {
-        dialog.showModal();
-      }
-    });
-  })();
-</script>
diff --git a/src/layouts/CompareLayout.astro b/src/layouts/CompareLayout.astro
index a5870d0..e35c2aa 100644
--- a/src/layouts/CompareLayout.astro
+++ b/src/layouts/CompareLayout.astro
@@ -52,2 +52,3 @@ const pageTitle = `${AppConfig.appName} vs ${entry.shortName} | Platform Compari
             data-open-modal="trial-modal"
+            data-cta-location="compare_page_header"
             class="inline-flex cursor-pointer items-center rounded-pill bg-brand px-8 py-4 text-base font-semibold text-white shadow-soft transition-transform"
@@ -188,2 +189,3 @@ const pageTitle = `${AppConfig.appName} vs ${entry.shortName} | Platform Compari
               data-open-modal="trial-modal"
+              data-cta-location="compare_page_footer"
               class="inline-flex cursor-pointer items-center rounded-pill bg-brand px-8 py-4 text-base font-semibold text-white shadow-soft transition-transform"
@@ -202,15 +204 @@ const pageTitle = `${AppConfig.appName} vs ${entry.shortName} | Platform Compari
 <ContactUsModal id="contact-us-modal" title="Contact Us" />
-
-<script is:inline>
-  (function () {
-    document.addEventListener('click', function (e) {
-      const btn = e.target.closest('[data-open-modal]');
-      if (!btn) return;
-      const modalId = btn.getAttribute('data-open-modal');
-      const dialog = document.getElementById(modalId);
-      if (dialog && typeof dialog.showModal === 'function') {
-        dialog.showModal();
-      }
-    });
-  })();
-</script>
diff --git a/src/layouts/Layout.astro b/src/layouts/Layout.astro
index 72dcd17..6115d54 100644
--- a/src/layouts/Layout.astro
+++ b/src/layouts/Layout.astro
@@ -26,11 +26,14 @@ const canonicalUrl = canonicalPath ? new URL(canonicalPath, AppConfig.urls.rootU
   <head>
-      <!-- Google tag (gtag.js) -->
-      <script async src="https://www.googletagmanager.com/gtag/js?id=G-P256SSXRW7"></script>
+      <!-- Google Tag Manager -->
       <script>
           window.dataLayer = window.dataLayer || [];
-          function gtag(){dataLayer.push(arguments);}
-          gtag('js', new Date());
-
-          gtag('config', 'G-P256SSXRW7');
       </script>
+      <script is:inline>
+          (function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
+          new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
+          j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
+          'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
+          })(window,document,'script','dataLayer','GTM-XXXXXXX');
+      </script>
+      <!-- End Google Tag Manager -->
     <meta charset="UTF-8" />
@@ -59,2 +62,12 @@ const canonicalUrl = canonicalPath ? new URL(canonicalPath, AppConfig.urls.rootU
   <body class="min-h-screen overflow-x-hidden">
+    <!-- Google Tag Manager (noscript) -->
+    <noscript>
+      <iframe
+        src="https://www.googletagmanager.com/ns.html?id=GTM-XXXXXXX"
+        height="0"
+        width="0"
+        style="display:none;visibility:hidden"
+      ></iframe>
+    </noscript>
+    <!-- End Google Tag Manager (noscript) -->
     <slot />
@@ -73,2 +86,69 @@ const canonicalUrl = canonicalPath ? new URL(canonicalPath, AppConfig.urls.rootU
   })()
+</script>
+
+<!--
+  Centralized "open modal" handler for every [data-open-modal] trigger on the
+  site (Hero, Guarantee, CallToAction, Pricing plans, Compare/Alternatives
+  pages). This replaces the near-identical copies that used to live in
+  index.astro, CompareLayout.astro, and AlternativesLayout.astro.
+
+  It also doubles as the trial conversion-intent tracker: whenever the
+  trial-modal is opened, it pushes a `trial_interest` event to the GTM
+  dataLayer, and - if the trigger button carries plan info (only the
+  pricing-plan buttons do) - remembers that plan in sessionStorage so
+  TrialLicenseForm.tsx can attach it to the `trial_signup` event once the
+  form is actually submitted successfully.
+-->
+<script is:inline>
+  (function () {
+    window.dataLayer = window.dataLayer || [];
+
+    document.addEventListener("click", function (e) {
+      const btn = e.target.closest("[data-open-modal]");
+      if (!btn) return;
+
+      const modalId = btn.getAttribute("data-open-modal");
+      const dialog = document.getElementById(modalId);
+
+      if (modalId === "trial-modal") {
+        const planName = btn.getAttribute("data-plan-name");
+        const planPrice = btn.getAttribute("data-plan-price");
+        const billingPeriod = btn.getAttribute("data-billing-period");
+        const currency = btn.getAttribute("data-currency");
+        const ctaLocation = btn.getAttribute("data-cta-location") || "unknown";
+
+        if (planName) {
+          // Clicked a pricing-plan CTA - remember the choice for the
+          // eventual trial_signup event.
+          sessionStorage.setItem(
+            "rb_trial_plan",
+            JSON.stringify({
+              plan_name: planName,
+              plan_price: planPrice || null,
+              billing_period: billingPeriod || null,
+              currency: currency || null,
+            })
+          );
+        } else {
+          // Opened from a generic CTA (Hero/Guarantee/etc.) - clear any
+          // stale plan choice from an earlier click so it isn't
+          // misattributed to this signup.
+          sessionStorage.removeItem("rb_trial_plan");
+        }
+
+        window.dataLayer.push({
+          event: "trial_interest",
+          cta_location: ctaLocation,
+          plan_name: planName || null,
+          plan_price: planPrice || null,
+          billing_period: billingPeriod || null,
+          currency: currency || null,
+        });
+      }
+
+      if (dialog && typeof dialog.showModal === "function") {
+        dialog.showModal();
+      }
+    });
+  })();
 </script>
\ No newline at end of file
diff --git a/src/pages/index.astro b/src/pages/index.astro
index 735818e..9d68325 100644
--- a/src/pages/index.astro
+++ b/src/pages/index.astro
@@ -37,15 +37 @@ import TrialLicenseModal from "@components/TrialLicenseModal.astro";
 
-
-<script is:inline>
-    (function () {
-        document.addEventListener('click', function (e) {
-            const btn = e.target.closest('[data-open-modal]');
-            if (!btn) return;
-            const modalId = btn.getAttribute('data-open-modal');
-            const dialog = document.getElementById(modalId);
-            if (dialog && typeof dialog.showModal === 'function') {
-                dialog.showModal();
-            }
-        });
-    })();
-</script>
\ No newline at end of file
